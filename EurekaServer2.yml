eureka:
  instance:
    hostname: EurekaServer2
    appname: eureka-cluster
  environment: local
  client:
    serviceUrl:
      defaultZone:
        http://EurekaServer1:8761/eureka
    registerWithEureka: true  
    fetchRegistry: true
