eureka:
  instance:
    hostname: EurekaServer
    appname: eureka-cluster
  environment: local
  client:
    serviceUrl:
      defaultZone:
        http://EurekaServer2:8762/eureka
    registerWithEureka: true 
    fetchRegistry: true
